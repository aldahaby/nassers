# generated by copy-web.mjs on every sync
www/
node_modules/
# native projects are created by `npx cap add` and are large; add them yourself if you want them
# tracked (you probably do, once you start setting icons and signing)
ios/
android/
