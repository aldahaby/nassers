import { EmptyState } from '@/ui';

// Placeholder until the stats milestone. Game logic for it already exists in `core/`.
export default function StatsScreen() {
  return <EmptyState icon="📊" title="Your stats" body="Sessions, streaks, focus minutes and lifetime coins will be charted here in the next milestone." />;
}
