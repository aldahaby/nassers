import { EmptyState } from '@/ui';

// Placeholder until the shop milestone. Game logic for it already exists in `core/`.
export default function ShopScreen() {
  return <EmptyState icon="🛍️" title="The shop" body="Toys, snacks, outfits and room decor are stocked and waiting. The storefront opens in the next milestone." />;
}
